# example / OPE

Copy the validated root-module pattern into this directory only after OPE inputs and controls are confirmed.

OPE must use a dedicated remote Terraform state, scoped OpenShift identity, protected CI environment, manual apply and the agreed approval gates.
