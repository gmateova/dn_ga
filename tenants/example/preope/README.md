# example / preOPE

Copy the DEV root-module pattern into this directory when preOPE values are confirmed.

This environment must use its own remote Terraform state and its own scoped OpenShift identity. Do not reuse DEV state or credentials.
